// Iterate over an array
const heroes = ['Captain Marvel', 'Iron Man', 'Hulk', 'Thor', 'Spider Man'];

for (let hero of heroes) {
    console.log(hero);
}

// Iterate over a string
for (let char of heroes[0]) {
    console.log(char);
}