/*
Creating an Array
There are two ways to create an array in JavaScript:

1. The array literal, which uses square brackets.
2. The array constructor, which uses the new keyword.

Let’s demonstrate how to create an array of shark species using the array literal, which is initialized with [].
*/

// Initialize array of shark species with array literal
let sharks = [
    "Hammerhead",
    "Great White",
    "Tiger",
];

console.log(sharks); // [ 'Hammerhead', 'Great White', 'Tiger' ]

// Initialize array of shark species with array constructor
let sharks2 = new Array(
	"Hammerhead",
	"Great White",
	"Tiger",
);

console.log(sharks); // [ 'Hammerhead', 'Great White', 'Tiger' ]
