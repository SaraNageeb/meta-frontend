const multiplyUs = (num1, num2) => num1 * num2; 

const subtractFromTen = num => {
	const diff = 10 - num; 
	return diff;
}

const printDate = date => {
	return date;
}

multiplyUs(2,6);
subtractFromTen(4);
printDate('1/16/18');